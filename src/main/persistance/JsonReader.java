package persistance;

import model.Calendar;

//Represents a tool that reads a JSON file into a calendar
public class JsonReader {
    
    public JsonReader() {

    }

    // EFFECTS: reads information from .json file from the file path and returns it in the form of a calendar
    public Calendar read(String filePath) {
        return null;
    }
}
